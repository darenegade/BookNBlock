package contract

import (
	"time"

	".."
)

type (
	HyperLedger struct {
		// ...?
	}
)

func (e *HyperLedger) IsAllowedAt(mieter tür.MieterID, t time.Time) (bool, error) {
	panic("not yet implemented")
}
