package contract

import (
	"time"

	".."
)

type (
	Ethereum struct {
		// ...?
	}
)

func (e *Ethereum) IsAllowedAt(mieter tür.MieterID, t time.Time) (bool, error) {
	panic("not yet implemented")
}
